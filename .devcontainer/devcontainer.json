{
    "name": "Haskell Environment",
    "image": "mcr.microsoft.com/devcontainers/universal:latest",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ghc cabal-install && cabal update",
    "customizations": {
        "vscode": {
            "extensions": [
                "haskell.haskell",
                "justusadam.language-haskell"
            ]
        }
    }
}
