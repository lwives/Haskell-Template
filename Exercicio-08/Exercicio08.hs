-- Usando funções de ordem superior ('map', 'fold'...) e lambda (i.e., não nomeadas), 
-- elabore uma função que realize o cálculo da Distância de Manhattan. Para tanto, 
-- a função recebe duas listas de números reais (representando vetores Euclidianos) 
-- e calcula a distância entre eles, conforme a descrição seguinte (tal definição 
-- leva em conta que os valores do vetor estão normalizados entre 0.0 e 1.0): 

-- distanciaManhattan = somatório das diferenças absolutas entre os elementos correspondentes

distanciaManhattan :: [Double] -> [Double] -> Double
distanciaManhattan ls1 ls2 = undefined -- Implemente
